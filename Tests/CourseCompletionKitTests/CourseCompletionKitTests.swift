import XCTest
@testable import CourseCompletionKit

final class CourseCompletionKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CourseCompletionKit().text, "Hello, World!")
    }
}
